import React, { useState, useEffect } from "react";
import { useDispatch, useSelector } from "react-redux";

const AuditorScreen = ({ history }) => {
    
  useEffect(() => {}, []);
  return <div></div>;
};

export default AuditorScreen;
